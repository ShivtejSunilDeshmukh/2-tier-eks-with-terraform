apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: devopspulse
  labels:
    app: devopspulse
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: devopspulse
    tier: backend
  ports:
    - port: 80
      targetPort: 8000
